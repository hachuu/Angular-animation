import { Routes } from '@angular/router';
import { ReactiveFormComponent } from './reactive-form.component';

export const ReactiveFormRoute: Routes = [
  { path: 'reactive-form', component: ReactiveFormComponent },
]